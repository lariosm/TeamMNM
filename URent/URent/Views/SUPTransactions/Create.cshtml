@model URent.Models.SUPTransaction

@{
    ViewBag.Title = "Create";
}

@if (Request.IsAuthenticated)
{
    Layout = "~/Views/Shared/_UserPageLayout.cshtml";
}

<div class="container">
    <h2>Rent this Item</h2>


    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div actions="/SUPTranctions/Create" class="form-horizontal" method="POST">
            <h4>SUPTransaction</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <div id="price" data-price="@ViewBag.dailyprice"></div>
            <div>
                <label>Date from</label>
                <p>
                    <input class="form-group form-control col-md-2" type="text" id="StartDate" name="StartDate" placeholder="MM/DD/YYYY" />
                </p>
                <label>Date to</label>
                <p>
                    <input class="form-group form-control col-md-2" type="text" id="EndDate" name="EndDate" placeholder="MM/DD/YYYY" />
                </p>
                <div id="TextBox3" typeof="hidden" val=""></div>

                <label>Total price</label>
                <span>
                    <input class="form-group form-control col-md-2" id="TotalPrice" name="TotalPrice" type="text" value="" readonly="readonly" />
                </span>

                <input type="submit" value="Rent this item" class="btn btn-primary" />
            </div>
        </div>
    }

    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryui")
    @Styles.Render("~/Content/cssjqueryui")
    @Scripts.Render("~/Scripts/Calendar.js")
    @Scripts.Render("~/Scripts/transactionPriceCalculator.js")
}
